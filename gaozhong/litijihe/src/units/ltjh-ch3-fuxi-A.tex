{\centering \nonumsubsection{A \hspace{1em} 组}}
\begin{xiaotis}

\xiaoti{}%
\begin{xiaoxiaotis}%
    \xxt[\xxtsep]{用正三角形做面可以组成几种多面角？为什么？}

    \xxt{用正方形做面可以组成几种多面角？为什么？}

    \xxt{用正五边形做面可以组成几种多面角？为什么？}

    \xxt{能否用正六边形做面组成三面角？为什么？}

\end{xiaoxiaotis}

\xiaoti{在直三面角 $S{-}ABC$ 的三个棱上，取 $SA = SB = SC = a$。}
\begin{xiaoxiaotis}

    \xxt{求直线AB与SC的距离；}

    \xxt{求点C与直线AB的距离；}

    \xxt{求以 $SAB$、$CAB$ 为面的二面角的大小。}

\end{xiaoxiaotis}


\xiaoti{已知正四面体所有面的中心，是一个正四面体的顶点。画出这个正四面体。}

\xiaoti{线段 $AC$、$BD$、$EF$ 相等，并且两两互相垂直平分于点 $O$。
    求证：$A$、$B$、$C$、$D$、$E$、$F$ 是正八面体的六个顶点。
}

\xiaoti{已知：一个简单多面体的各个顶点都有三条棱。 求证： $V = 2F - 4$。}

\end{xiaotis}
